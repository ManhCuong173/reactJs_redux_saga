const styles = theme => ({
  card: {
    fontStyle: 'italic',
    // boxShadow: `2px 2px 5px ${theme.color.primary}`
    margin: '10px 0',
  },
  cardActions: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
});
export default styles;
