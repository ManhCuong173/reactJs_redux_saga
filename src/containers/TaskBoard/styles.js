const styles = theme => ({
  mainLayout: {
    padding: 30,
  },
  button: {
    fontFamily: theme.typography.fontFamily,
    padding: 10,
    margin: 20,
  },
  shape: {
    color: theme.color.primary,
  },
});

export default styles;
